name: todo_list_app
description: A simple Todo List app.
publish_to: 'none' # Remove this line if you want to publish to pub.dev
version: 1.0.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  
 # تأكد من أن النسخة متوافقة مع نسخة Flutter المستخدمة

dev_dependencies:
  flutter_test:
    sdk: flutter

  cupertino_icons: ^1.0.2
flutter:
  uses-material-design: true
